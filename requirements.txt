# ============================================
# ProtonAI - قائمة المكتبات المطلوبة
# ============================================

# --- مكتبات التشغيل ---
# عقود البيانات والتحقق (Pydantic الإصدار الثاني)
pydantic>=2.0

# --- مكتبات الاختبار ---
# إطار الاختبارات الآلية (يشغّل 96 اختبار)
pytest>=7.0

# ملاحظة: باقي الوحدات (ingestion, split, lineage,
# normalizers, reporters, baseline_model, pipeline)
# تعتمد فقط على مكتبات بايثون المدمجة،
# لذلك المنصة خفيفة وسريعة التثبيت.
